{
  "name": "compras-verdu",
  "private": true,
  "version": "1.0.0",
  "description": "Sistema de compras a proveedores - monorepo",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:db": "docker compose -f docker-compose.db.yml up -d",
    "start": "powershell -ExecutionPolicy Bypass -File scripts/start-dev.ps1",
    "start:all": "powershell -ExecutionPolicy Bypass -File scripts/start-dev.ps1",
    "build": "npm run build --prefix frontend",
    "apk": "npm run apk --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "ci": "npm run lint --prefix backend && npm run test --prefix backend && npm run lint --prefix frontend && npm run build --prefix frontend && npm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
